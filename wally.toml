[package]
name = "samassekou/penalty-kings"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
# ProfileStore (the data layer) is vendored directly as a single MIT module at
# src/server/ProfileStore.luau -- no version pinning, no registry dependency.
# Add future Wally packages here and run `wally install`.
